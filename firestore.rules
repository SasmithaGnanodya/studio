
/**
 * This Firestore Security Ruleset enforces a security model where all authenticated users can read
 * all reports and layouts. Any authenticated user can update any report, but only admins can
 * manage layouts and view report history.
 *
 * Core Philosophy:
 * The security model is designed to be highly collaborative. Reading is open to all signed-in users.
 * Report updates are also open to all signed-in users to allow for multi-user editing workflows.
 * Layout modifications and history viewing are privileged actions, restricted to admins.
 *
 * Data Structure:
 * - /users/{userId}: Private user profile data. Only the owner can manage.
 * - /layouts/{layoutId}: Stores versioned layouts. Readable by all, writable only by an admin.
 * - /reports/{reportId}: Individual report documents. Readable and writable by all signed-in users, but can only be
 *   created by a user setting themselves as the owner.
 * - /reports/{reportId}/history/{historyId}: Read-only audit trail of report saves. Only admins can read.
 * - /config/settings: App-wide settings, like the private data password. Writable only by admins.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ------------------------------------------------------------------------
    // Helper Functions
    // ------------------------------------------------------------------------

    function isSignedIn() {
      return request.auth != null;
    }
    
    function isAdmin() {
      return isSignedIn() && request.auth.token.email in ['sasmithagnanodya@gmail.com', 'supundinushaps@gmail.com', 'caredrivelk@gmail.com'];
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    function isCreatingReportAsOwner() {
      return isSignedIn() && request.auth.uid == request.resource.data.userId;
    }

    // ------------------------------------------------------------------------
    // Collection Rules
    // ------------------------------------------------------------------------

    /**
     * @description Rules for app-wide configuration settings.
     * @path /config/settings
     */
    match /config/settings {
      allow get: if isSignedIn();
      allow create, update, delete: if isAdmin();
    }

    /**
     * @description Rules for a user's own profile document.
     * @path /users/{userId}
     */
    match /users/{userId} {
      allow get, update, delete: if isOwner(userId);
      allow list: if false; // Prevent listing all users
      allow create: if isOwner(userId);
    }
    
    /**
     * @description Rules for the layout documents.
     * Any signed-in user can read any layout version.
     * Only an admin can create, update, or delete layouts.
     * @path /layouts/{layoutId}
     */
    match /layouts/{layoutId} {
      allow get, list: if isSignedIn();
      allow create, update, delete: if isAdmin();
    }

    /**
     * @description Rules for individual report documents.
     * Contains a `history` subcollection for auditing.
     * @path /reports/{reportId}
     */
    match /reports/{reportId} {
      // Any signed-in user can read a report or list the collection.
      allow get, list: if isSignedIn();
      
      // A user can create a report if they are setting themselves as the owner.
      allow create: if isCreatingReportAsOwner();
      
      // Any signed-in user can update a report. Admins can delete.
      allow update: if isSignedIn();
      allow delete: if isAdmin();
      
      /**
       * @description Rules for the report history subcollection.
       * This is an audit trail, so it's write-only for the system and read-only for admins.
       * @path /reports/{reportId}/history/{historyId}
       */
      match /history/{historyId} {
        allow get, list: if isAdmin();
        allow create: if isSignedIn(); // Writes are handled via transaction in backend logic
        allow update, delete: if false; // History is immutable
      }
    }
  }
}
