export type Field = {
  id: string;
  page: number;
  x: number; // percentage
  y: number; // percentage
  width: number; // percentage
  height: number; // percentage
  value: string;
  type: string;
  isRequired: boolean;
};
